/**
 * @summary this function accepts 2 number-typed parameters, do add operation and return the total sum of them
 * @param a number
 * @param b number
 * @returns number
 */
function add(a: number, b: number): number {
  return a + b;
}

console.log(add(1, 4));

export default add;
